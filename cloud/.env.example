# AgentSDK Cloud Backend（agentsdk.run）

PORT=3000
NODE_ENV=development

# 本地 Agent 默认工作目录（VPS 上须存在）；remote-cli 可不传 workingDirectory
DEFAULT_AGENT_CWD=/home/xinference/agent-workspace

# 可选：服务端代填 Cursor Key（请求头仍可覆盖）
# CURSOR_API_KEY=
